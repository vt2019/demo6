apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/jenkinsx-pw1/demo6:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/jenkinsx-pw1/demo6:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: demo6
        chartPath: charts/demo6
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/jenkinsx-pw1/demo6"
          image.tag: "{{.DIGEST_HEX}}"
